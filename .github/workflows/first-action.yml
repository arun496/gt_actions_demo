name: bash-actions

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: |
          chmod +x scripts/demo.sh
          sh scripts/demo.sh
        shell: bash